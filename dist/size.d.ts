/** Semantic Props container sizes in `rem` units. */
export declare const container: Record<string, `${number}rem`>;
/** Semantic Props sizes in `rem` or `em` units. */
export declare const size: Record<string, `${number}${"rem" | "em"}`>;
