/** Semantic Props for CSS `scaleX()` transform function. */
export declare const scaleX: Record<string, `scaleX(${number})`>;
/** Semantic Props for CSS `scaleY()` transform function. */
export declare const scaleY: Record<string, `scaleY(${number})`>;
/** Semantic Props for CSS `scale()` transform function. */
export declare const scale: Record<string, `scale(${number})`>;
