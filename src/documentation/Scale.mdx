import Docs from "../components/Docs";

<Docs
	id="scale"
	title="Scale Properties"
	desc="Semantic Props for CSS scale property."
	props={{
		"scale": [
			"--scale-3x-out",
			"--scale-2x-out",
			"--scale-x-out",
			"--scale-out",
			"--scale-in",
			"--scale-x-in",
			"--scale-2x-in",
			"--scale-3x-in"
		]
}} />